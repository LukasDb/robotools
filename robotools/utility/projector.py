import background_monitor as bm

class DemoProjector:
    #enter values

class Projector(bm.BackgroundMonitor):
    """Projector class, the position is arbitrary"""
    def __init__(self) -> None:
        bm.BackgroundMonitor.__init__(self, "Pattern Projector")

    def set_to_pattern(self):
            """ I would like to create an array of the different patterns
            in the folder, through wich the code then iterates and takes a picture each"""
    def rotate_pattern():
        """ each pattern could then also be rotated to create a new pattern"""
    def black_screen():
         

    